// Escribe tu código aquí:
const frase = "La ensalada está hecha con patatas"
const palabraRemplazada = frase.replace("patatas", "batatas")

console.log(palabraRemplazada)

// Resultado: La ensalada está hecha con batatas